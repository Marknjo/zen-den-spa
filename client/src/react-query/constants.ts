interface QueryKeys {
  treatments: string;
  appointments: string;
  user: string;
  staff: string;
}

export const queryKeys: QueryKeys = {
  treatments: 'treatments',
  appointments: 'appointments',
  user: 'user',
  staff: 'staff',
};
